---
const currentYear = new Date().getFullYear();

const links = {
  product: [
    { text: "Features", href: "#features" },
    { text: "How It Works", href: "#how-it-works" },
    { text: "Join Waitlist", href: "#email-signup" },
    { text: "FAQ", href: "#faq" }
  ],
  company: [
    { text: "About Us", href: "/about" },
    { text: "Contact", href: "mailto:lilyaddy15@icloud.com" }
  ]
};
---

<footer class="bg-deep-navy text-white py-20">
  <div class="container px-6 md:px-8">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12 mb-12">
      <div>
        <h3 class="text-2xl font-bold mb-6">Amplifly</h3>
        <p class="text-white/70 mb-6">
          Smart support for neurodivergent minds, powered by AI and biometric data.
        </p>
      </div>

      <div>
        <h4 class="font-semibold mb-6">Product</h4>
        <ul class="space-y-4">
          {links.product.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold mb-6">Company</h4>
        <ul class="space-y-4">
          {links.company.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="border-t border-white/10 pt-8">
      <p class="text-center text-white/70">
        © {currentYear} Amplifly. All rights reserved.
      </p>
    </div>
  </div>
</footer>