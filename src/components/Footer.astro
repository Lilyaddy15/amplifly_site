---
const currentYear = new Date().getFullYear();

const links = {
  product: [
    { text: "What You Get", href: "#features" },
    { text: "How It Works", href: "#how-it-works" },
    { text: "Join Waitlist", href: "#email-signup" },
    { text: "FAQ", href: "#faq" }
  ],
  company: [
    { text: "About Us", href: "/about" },
    { text: "Contact", href: "mailto:lily@amplifly.me" }
  ],
  legal: [
    { text: "Privacy Policy", href: "/privacy-policy" },
    { text: "Terms of Service", href: "/terms-of-service" }
  ]
};

const socialLinks = [
  { 
    text: "Instagram", 
    href: "https://www.instagram.com/amplifly_app?igsh=emZ4YWd2ZnAwZHZy&utm_source=qr",
    icon: "📷"
  },
  { 
    text: "TikTok", 
    href: "https://www.tiktok.com/@amplifly_app?_t=ZT-8xXFsk6j3eQ&_r=1",
    icon: "🎵"
  },
  { 
    text: "LinkedIn", 
    href: "https://www.linkedin.com/company/amplifly-app/",
    icon: "💼"
  }
];
---

<footer class="bg-deep-navy text-white py-20">
  <div class="container px-6 md:px-8">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12">
      <div>
        <h3 class="text-2xl font-bold mb-6">Amplifly</h3>
        <p class="text-white/70 mb-6">
          Smart support for neurodivergent minds, powered by AI and biometric data.
        </p>
        <a href="mailto:lily@amplifly.me" class="inline-block bg-bright-blue text-white px-6 py-3 rounded-full text-sm font-bold hover:bg-opacity-90 transition-all">
          Contact Us
        </a>
      </div>

      <div>
        <h4 class="font-semibold mb-6">Product</h4>
        <ul class="space-y-4">
          {links.product.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold mb-6">Company</h4>
        <ul class="space-y-4">
          {links.company.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold mb-6">Legal</h4>
        <ul class="space-y-4">
          {links.legal.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="border-t border-white/10 pt-8">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <p class="text-white/70 mb-4 md:mb-0">
          © {currentYear} Amplifly. All rights reserved.
        </p>
        <div class="flex space-x-6">
          {socialLinks.map(link => (
            <a 
              href={link.href} 
              target="_blank" 
              rel="noopener noreferrer"
              class="text-white/70 hover:text-white transition-colors flex items-center space-x-2"
              title={link.text}
            >
              <span class="text-lg">{link.icon}</span>
              <span class="hidden sm:inline">{link.text}</span>
            </a>
          ))}
        </div>
      </div>
    </div>
  </div>
</footer>